<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="recipe">
		<!-- 레시피 정보 입력 -->
	<insert id="insert" parameterType="vo">
		insert into recipe(cate, con_num, main_name, title, mate, pro) 
		values (#{cate}, #{con_num}, #{main_name}, #{title}, #{mate}, #{pro})
	</insert>

		<!-- 레시피 출력 -->
	<select id="select" resultType="vo">
		select * from recipe
	</select>

		<!-- 레시피 갯수 카운트 -->	
	<select id="count" resultType="int">
		select count(*) from recipe
	</select>
	
		<!-- 레시피 카테고리 선택 -->
	<select id = "count1" resultType="int">
		select count(*) from recipe where cate = 23
	</select>

		<!-- 레시피 조리법 이미지 입력 -->
	<insert id="imginsert" parameterType="ivo">
		insert into recipe_pro (con_num, img1, img2)
		values (#{con_num}, #{img1}, #{img2})
	</insert>

</mapper>